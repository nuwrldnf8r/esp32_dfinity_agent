#ifndef agent_h
#define agent_h

#include <Arduino.h>
#include <vector>



class dfinityAgent {
public:
    /*
    uint8_t ever_le(uint64_t n, uint8_t* buffer);
    uint8_t biggie(size_t k, uint64_t n, uint8_t* buffer);
    uint64_t unbiggie(uint8_t* bytes, size_t len);
    uint8_t* cbor_tc(uint64_t ty, uint64_t n, size_t& length);

    CBOR* cborLookup(const String& k, const std::vector<std::pair<CBOR*, CBOR*>>& map);
    void cborEncode(const CBOR* cbor, uint8_t* buffer, size_t length);
    */
};

#endif
